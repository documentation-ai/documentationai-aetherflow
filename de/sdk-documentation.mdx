---
title: SDK-Dokumentation
description: Integrieren Sie AetherFlow programmgesteuert mit unseren SDKs fuer JavaScript, Python und REST-APIs
---

## SDK-Uebersicht

Interagieren Sie programmgesteuert mit AetherFlow mithilfe unserer offiziellen SDKs und REST-APIs.

<Callout kind="info">
  SDKs sind fuer JavaScript, Python und Go verfuegbar. REST-API-Zugriff steht fuer alle Programmiersprachen zur Verfuegung.
</Callout>

## Installation

Beginnen Sie mit den AetherFlow-SDKs in Ihrer bevorzugten Sprache.

<Tabs>
  <Tab title="JavaScript/Node.js" icon="javascript">
    ```bash
    npm install @aetherflow/sdk
    # oder
    yarn add @aetherflow/sdk
    ```
  </Tab>

  <Tab title="Python" icon="python">
    ```bash
    pip install aetherflow-sdk
    # oder
    poetry add aetherflow-sdk
    ```
  </Tab>

  <Tab title="Go" icon="go">
    ```bash
    go get github.com/aetherflow/aetherflow-go-sdk
    ```
  </Tab>
</Tabs>

## Authentifizierung

Authentifizieren Sie sich sicher bei AetherFlow mit API-Schluesseln oder JWT-Token.

<Expandable title="API-Schluessel-Einrichtung">
```javascript
import { AetherFlow } from '@aetherflow/sdk';

const client = new AetherFlow({
  apiKey: 'your_api_key_here'
});
```

```python
from aetherflow import AetherFlow

client = AetherFlow(api_key='your_api_key_here')
```

```go
package main

import (
    "github.com/aetherflow/aetherflow-go-sdk"
)

func main() {
    client := aetherflow.NewClient(&aetherflow.Config{
        APIKey: "your_api_key_here",
    })
}
```
</Expandable>

<Expandable title="JWT-Token-Einrichtung">
```javascript
const client = new AetherFlow({
  jwtToken: 'your_jwt_token_here'
});
```

```python
client = AetherFlow(jwt_token='your_jwt_token_here')
```

```go
client := aetherflow.NewClient(&aetherflow.Config{
    JWTToken: "your_jwt_token_here",
})
```
</Expandable>

## Workflows-API

Erstellen, verwalten und fuehren Sie Workflows programmgesteuert aus.

### Workflows erstellen

<CodeGroup tabs="JavaScript,Python,Go">
```javascript
const workflow = await client.workflows.create({
  name: 'Email Processor',
  prompt: 'When I receive an email, summarize it and post to Slack',
  integrations: ['gmail', 'slack']
});

console.log(`Created workflow: ${workflow.id}`);
```

```python
workflow = client.workflows.create(
    name='Email Processor',
    prompt='When I receive an email, summarize it and post to Slack',
    integrations=['gmail', 'slack']
)

print(f"Created workflow: {workflow.id}")
```

```go
workflow, err := client.Workflows.Create(context.Background(), &aetherflow.WorkflowRequest{
    Name: "Email Processor",
    Prompt: "When I receive an email, summarize it and post to Slack",
    Integrations: []string{"gmail", "slack"},
})

if err != nil {
    log.Fatal(err)
}

fmt.Printf("Created workflow: %s\n", workflow.ID)
```
</CodeGroup>

### Workflows auflisten

<CodeGroup tabs="JavaScript,Python,Go">
```javascript
const workflows = await client.workflows.list({
  limit: 10,
  status: 'active'
});

workflows.forEach(wf => {
  console.log(`${wf.name}: ${wf.status}`);
});
```

```python
workflows = client.workflows.list(limit=10, status='active')

for wf in workflows:
    print(f"{wf.name}: {wf.status}")
```

```go
workflows, err := client.Workflows.List(context.Background(), &aetherflow.ListOptions{
    Limit:  10,
    Status: "active",
})

if err != nil {
    log.Fatal(err)
}

for _, wf := range workflows {
    fmt.Printf("%s: %s\n", wf.Name, wf.Status)
}
```
</CodeGroup>

### Workflows ausfuehren

<CodeGroup tabs="JavaScript,Python,Go">
```javascript
const execution = await client.workflows.execute(workflowId, {
  customData: {
    email_subject: 'Important Update',
    priority: 'high'
  }
});

console.log(`Execution started: ${execution.id}`);
```

```python
execution = client.workflows.execute(
    workflow_id,
    custom_data={
        'email_subject': 'Important Update',
        'priority': 'high'
    }
)

print(f"Execution started: {execution.id}")
```

```go
execution, err := client.Workflows.Execute(context.Background(), workflowID, &aetherflow.ExecuteOptions{
    CustomData: map[string]interface{}{
        "email_subject": "Important Update",
        "priority": "high",
    },
})

if err != nil {
    log.Fatal(err)
}

fmt.Printf("Execution started: %s\n", execution.ID)
```
</CodeGroup>

## Analyse-API

Rufen Sie Leistungsmetriken und Analysedaten ab.

<Expandable title="Workflow-Metriken abrufen">
```javascript
const metrics = await client.analytics.getWorkflowMetrics(workflowId, {
  timeframe: 'last_30_days',
  metrics: ['success_rate', 'avg_runtime', 'execution_count']
});

console.log(`Success Rate: ${metrics.success_rate}%`);
```

```python
metrics = client.analytics.get_workflow_metrics(
    workflow_id,
    timeframe='last_30_days',
    metrics=['success_rate', 'avg_runtime', 'execution_count']
)

print(f"Success Rate: {metrics.success_rate}%")
```
</Expandable>

## Integrations-API

Verwalten Sie verbundene Anwendungen und Dienste.

### Integrationen auflisten

<CodeGroup tabs="JavaScript,Python">
```javascript
const integrations = await client.integrations.list();

integrations.forEach(integration => {
  console.log(`${integration.name}: ${integration.status}`);
});
```

```python
integrations = client.integrations.list()

for integration in integrations:
    print(f"{integration.name}: {integration.status}")
```
</CodeGroup>

### Integration testen

<CodeGroup tabs="JavaScript,Python">
```javascript
const testResult = await client.integrations.test(integrationId);

if (testResult.success) {
  console.log('Integration is working correctly');
} else {
  console.log(`Integration error: ${testResult.error}`);
}
```

```python
test_result = client.integrations.test(integration_id)

if test_result.success:
    print('Integration is working correctly')
else:
    print(f'Integration error: {test_result.error}')
```
</CodeGroup>

## Fehlerbehandlung

Robuste Fehlerbehandlungsmuster fuer eine zuverlaessige SDK-Nutzung.

<Expandable title="Beispiele zur Fehlerbehandlung">
```javascript
try {
  const workflow = await client.workflows.create(workflowData);
} catch (error) {
  if (error.code === 'VALIDATION_ERROR') {
    console.log('Invalid workflow configuration');
  } else if (error.code === 'RATE_LIMITED') {
    console.log('Rate limit exceeded, retrying...');
    // Wiederholungslogik implementieren
  } else {
    console.log(`Unexpected error: ${error.message}`);
  }
}
```

```python
try:
    workflow = client.workflows.create(workflow_data)
except ValidationError as e:
    print('Invalid workflow configuration')
except RateLimitError as e:
    print('Rate limit exceeded, retrying...')
    # Wiederholungslogik implementieren
except Exception as e:
    print(f'Unexpected error: {str(e)}')
```
</Expandable>

## Erweiterte Funktionen

Nutzen Sie erweiterte SDK-Moeglichkeiten fuer komplexe Automatisierungsszenarien.

### Webhook-Verwaltung

<CodeGroup tabs="JavaScript,Python">
```javascript
// Webhook-Endpunkt erstellen
const webhook = await client.webhooks.create({
  url: 'https://myapp.com/webhook',
  events: ['workflow.completed', 'workflow.failed'],
  secret: 'webhook_secret'
});

// Webhooks auflisten
const webhooks = await client.webhooks.list();
```

```python
# Webhook-Endpunkt erstellen
webhook = client.webhooks.create(
    url='https://myapp.com/webhook',
    events=['workflow.completed', 'workflow.failed'],
    secret='webhook_secret'
)

# Webhooks auflisten
webhooks = client.webhooks.list()
```
</CodeGroup>

### Batch-Vorgaenge

<Expandable title="Batch-Workflow-Erstellung">
```javascript
const workflowConfigs = [
  { name: 'Email Processor', prompt: 'Process incoming emails' },
  { name: 'Lead Router', prompt: 'Route sales leads' },
  { name: 'Report Generator', prompt: 'Generate daily reports' }
];

const results = await client.workflows.createBatch(workflowConfigs);

results.forEach((result, index) => {
  if (result.success) {
    console.log(`Created: ${result.workflow.name}`);
  } else {
    console.log(`Failed to create ${workflowConfigs[index].name}: ${result.error}`);
  }
});
```

```python
workflow_configs = [
    {'name': 'Email Processor', 'prompt': 'Process incoming emails'},
    {'name': 'Lead Router', 'prompt': 'Route sales leads'},
    {'name': 'Report Generator', 'prompt': 'Generate daily reports'}
]

results = client.workflows.create_batch(workflow_configs)

for i, result in enumerate(results):
    if result.success:
        print(f"Created: {result.workflow.name}")
    else:
        print(f"Failed to create {workflow_configs[i]['name']}: {result.error}")
```
</Expandable>

## SDK-Referenz

Vollstaendige API-Referenz fuer alle SDK-Methoden und -Parameter.

### Workflows

| Methode | Parameter | Beschreibung |
|---------|-----------|--------------|
| `create(config)` | name, prompt, integrations | Einen neuen Workflow erstellen |
| `list(options)` | limit, status, search | Workflows mit Filterung auflisten |
| `get(id)` | workflow_id | Workflow-Details abrufen |
| `update(id, config)` | workflow_id, updates | Workflow-Konfiguration aktualisieren |
| `delete(id)` | workflow_id | Einen Workflow loeschen |
| `execute(id, options)` | workflow_id, data | Einen Workflow ausfuehren |

### Analysen

| Methode | Parameter | Beschreibung |
|---------|-----------|--------------|
| `getWorkflowMetrics(id, options)` | workflow_id, timeframe, metrics | Workflow-Leistungsmetriken abrufen |
| `getSystemMetrics(options)` | timeframe, metrics | Systemweite Analysen abrufen |
| `exportData(options)` | format, date_range | Analysedaten exportieren |

### Integrationen

| Methode | Parameter | Beschreibung |
|---------|-----------|--------------|
| `list()` | - | Alle Integrationen auflisten |
| `get(id)` | integration_id | Integrationsdetails abrufen |
| `test(id)` | integration_id | Integrationskonnektivitaet testen |
| `configure(id, config)` | integration_id, config | Integrationseinstellungen aktualisieren |

## Ratenbegrenzung

Verstehen und behandeln Sie API-Ratenlimits effektiv.

<Expandable title="Behandlung von Ratenlimits">
```javascript
class RateLimitedClient {
  async executeWithRetry(operation, maxRetries = 3) {
    for (let attempt = 1; attempt <= maxRetries; attempt++) {
      try {
        return await operation();
      } catch (error) {
        if (error.code === 'RATE_LIMITED' && attempt < maxRetries) {
          const delay = Math.pow(2, attempt) * 1000; // Exponential backoff
          await new Promise(resolve => setTimeout(resolve, delay));
          continue;
        }
        throw error;
      }
    }
  }
}

// Verwendung
const client = new RateLimitedClient();
const result = await client.executeWithRetry(
  () => sdk.workflows.create(workflowConfig)
);
```

```python
import time
import asyncio

class RateLimitedClient:
    async def execute_with_retry(self, operation, max_retries=3):
        for attempt in range(1, max_retries + 1):
            try:
                return await operation()
            except RateLimitError as e:
                if attempt < max_retries:
                    delay = 2 ** attempt  # Exponential backoff
                    await asyncio.sleep(delay)
                    continue
                raise e

# Verwendung
client = RateLimitedClient()
result = await client.execute_with_retry(
    lambda: sdk.workflows.create(workflow_config)
)
```
</Expandable>

## Best Practices

Optimieren Sie Ihre SDK-Nutzung fuer Leistung und Zuverlaessigkeit.

<Columns cols={3}>
  <Card title="Verbindungsverwaltung" icon="server">
    Wiederverwenden Sie Client-Instanzen und implementieren Sie Connection Pooling.
  </Card>
  <Card title="Fehlerbehandlung" icon="alert-triangle">
    Implementieren Sie umfassende Fehlerbehandlung und Wiederholungslogik.
  </Card>
  <Card title="Ratenbegrenzung" icon="clock">
    Respektieren Sie API-Limits und implementieren Sie exponentielles Backoff.
  </Card>
  <Card title="Protokollierung" icon="file-text">
    Protokollieren Sie API-Aufrufe fuer Debugging und Monitoring.
  </Card>
  <Card title="Tests" icon="flask">
    Schreiben Sie Unit-Tests fuer Ihre SDK-Integrationen.
  </Card>
  <Card title="Versionierung" icon="tag">
    Fixieren Sie SDK-Versionen und planen Sie Upgrades sorgfaeltig.
  </Card>
</Columns>

## SDK-Updates und Changelog

Bleiben Sie mit den neuesten SDK-Funktionen und Verbesserungen auf dem neuesten Stand.

<Expandable title="Versionskompatibilitaet">
- **SDK v2.x**: Kompatibel mit AetherFlow API v2
- **SDK v1.x**: Kompatibel mit AetherFlow API v1 (veraltet)
- **Migrationsanleitung**: Verfuegbar fuer das Upgrade von v1 auf v2
</Expandable>

<Expandable title="Aktuelle Updates">
- **v2.1.0**: Batch-Vorgaenge hinzugefuegt und Fehlerbehandlung verbessert
- **v2.0.0**: Umfassende Neuschreibung mit async/await-Unterstuetzung und neuen Analyse-APIs
- **v1.5.0**: Webhook-Verwaltung und benutzerdefinierte Integrationen hinzugefuegt
</Expandable>

Die AetherFlow-SDKs bieten leistungsstarken programmgesteuerten Zugriff auf alle Plattformfunktionen und ermoeglichen eine nahtlose Integration in Ihre Anwendungen.
